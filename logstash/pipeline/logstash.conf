input {
	beats {
		port => 5044
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "http://172.17.48.1:9200"
		user => "elastic"
		password => "${LOGSTASH_INTERNAL_PASSWORD}"
	}
}
